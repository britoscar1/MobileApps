<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Demo_FinalProject.ViewModels"
    xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
    x:Class="Demo_FinalProject.Views.EscanerPage"
    x:DataType="vm:EscanerViewModel"
    Title="Escanear QR"
    BackgroundColor="{StaticResource Background}">

    <Grid RowDefinitions="*,Auto">

        <zxing:CameraBarcodeReaderView
            x:Name="BarcodeReader"
            IsDetecting="{Binding Escaneando}"
            BarcodesDetected="OnBarcodesDetected" />

        <VerticalStackLayout Grid.Row="1" Padding="16" Spacing="8"
                             BackgroundColor="{StaticResource Surface}">

            <Label Text="{Binding Estado}" FontSize="16"
                   TextColor="{Binding RegistroExitoso, Converter={StaticResource BoolToSuccessColorConverter}}"
                   FontAttributes="Bold" HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
